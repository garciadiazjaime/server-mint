{
  "name": "server-mint",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server",
    "lint": "npx eslint .",
    "start": "node server",
    "docker:build": "docker build -t garciadiazjaime/server-mint . --platform linux/amd64",
    "docker:push": "docker push garciadiazjaime/server-mint",
    "deploy": "npm run docker:build && npm run docker:push && echo 'docker pull garciadiazjaime/server-mint'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.30.0",
    "nodemon": "^2.0.20"
  }
}
